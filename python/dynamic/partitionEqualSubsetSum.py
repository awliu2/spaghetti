from typing import List

class Solution:
    def canPartition(self, nums: List[int]) -> bool:
        # total_sum = sum(nums)
        # if total_sum % 2 == 1:
        #     return False
        
        # sub_sum = total_sum // 2
        # dp = [sub_sum - nums[i] for i in len(nums)]

        # for i in len(nums):
        #     ...
        ...




nums =[1,5,11,5]

print(Solution().canPartition(nums))